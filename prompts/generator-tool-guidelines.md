## Guidelines for Tools
- Use Bash for running commands (e.g., `npm install`, `npx`, etc.).
- Use file-writing tools to create or modify code files.
- You can create multiple files in one iteration.
- If a scaffold command is recommended in the Plan, execute it first.